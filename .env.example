# Public defaults only. Never commit real credentials.
CI_ENVIRONMENT = production

# Analytics uses MySQL and the existing application timezone.
# Keep the existing server database configuration.
# Run analytics:prune from aaPanel Cron every hour for 30-day retention.
