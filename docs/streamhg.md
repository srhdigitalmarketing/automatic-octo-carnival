# StreamHG integration removed

StreamHG is no longer offered or queried by the application. Run php spark migrate after updating to delete saved StreamHG API entries and detach their link associations. Existing video URLs are preserved and remain ordinary stream links. UPNShare, R2, custom HTTP hosts and VOD integrations remain available.
