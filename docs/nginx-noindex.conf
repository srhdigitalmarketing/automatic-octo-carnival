# Include inside the aaPanel website server block.
# Repeat in any child location that declares its own add_header directives.
add_header X-Robots-Tag "noindex, nofollow, noimageindex, nosnippet" always;
